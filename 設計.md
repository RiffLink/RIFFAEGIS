# 完全要塞型・耐量子電子署名サービス 基本設計書 (RiffAegis)

---

## 目次
1. [システム全体構成（ゼロ知識・デタッチド監査アーキテクチャ）](#1-システム全体構成ゼロ知識デタッチド監査アーキテクチャ)
2. [契約書作成・組版エンジン（Markdown Contract & Signature Fusion）](#2-契約書作成組版エンジンmarkdown-contract--signature-fusion)
3. [日本語旧字体・異体字対応およびサロゲートペア正規化仕様](#3-日本語旧字体異体字対応およびサロゲートペア正規化仕様)
4. [データベース設計（Supabase / PostgreSQL）](#4-データベース設計supabase--postgresql)
5. [暗号・セキュリティ仕様（NIST FIPS 204 ML-DSA & WebAuthn）](#5-暗号セキュリティ仕様nist-fips-204-ml-dsa--webauthn)
6. [トリプル外部アンカー・タイムスタンプ仕様](#6-トリプル外部アンカータイムスタンプ仕様)
7. [合意締結証明書（Audit Certificate）仕様・法廷証拠力](#7-合意締結証明書audit-certificate仕様法廷証拠力)
8. [APIエンドポイント詳細仕様](#8-apiエンドポイント詳細仕様)
9. [認可モデル・セッショントークン（JWT）仕様](#9-認可モデルセッショントークンjwt仕様)
10. [データリテンション・運用・バッチ処理](#10-データリテンション運用バッチ処理)
11. [メール送信基盤](#11-メール送信基盤)
12. [環境変数・シークレット管理](#12-環境変数シークレット管理)
13. [依存パッケージ・テスト検証スイート](#13-依存パッケージテスト検証スイート)

---

## 1. システム全体構成（ゼロ知識・デタッチド監査アーキテクチャ）

本システムは**「通常運用において運営者（サーバー）すら契約書本文を閲覧できないクライアントサイド暗号化（E2EE）」**と**「地球規模の分散インフラによる多重耐改ざん保証」**を前提とします。  
原本PDFを一切変更せず、独立した「合意締結証明書（Audit Certificate）」を発行する**デタッチド（分離型）契約パケット方式**を採用します。

> [!NOTE]
> **ゼロ知識アーキテクチャに関する誠実な定義**  
> Webブラウザ完結型の暗号化サービスであるため、「サーバーが配信するJavaScript自体の改ざんリスク」をゼロにはできません。本設計では**厳格なCSP（Content Security Policy）**による外部通信の完全制限、**SRI（Subresource Integrity）**、および**オープンソース再現可能ビルド（Reproducible Build）**を組み合わせ、運営者やインフラ侵入者によるコード改ざんを極小化・検知可能にする設計をとります。

> [!CAUTION]
> **脅威モデルの前提条件：共有チャネルの安全性**  
> 本システムのE2EEは「サーバー（運営者）に対する」ゼロ知識性を保証するものです。暗号鍵を含む署名URLは甲から乙へSlack / Teams / メール等で直接共有されるため、**共有チャネル自体の傍受リスクには甲の責任で対処する必要があります**。機密性の高い契約では、Signal等のE2Eで暗号化されたメッセンジャーの利用を強く推奨するガイダンスをUIに常設表示します。

### ① 全体シーケンス図

```text
[甲 (作成者ブラウザ)]
   │ ① 文書準備：
   │    ・既存PDFアップロード（マジックバイト %PDF- 検査、上限20MB）
   │    ・または ブラウザ内Markdownエディタで直接作成 ＆ 高精度A4 PDF自動組版
   │ ② 当事者情報（甲・乙の会社名・氏名・住所・肩書）の設定
   │ ③ 署名合意シート融合（fuseSignatureSheet）：未締結プレビュー状態のPDF生成
   │ ④ クライアントサイド暗号化 (AES-GCM-256)
   │ ⑤ 二重ハッシュ計算 (SHA-256 + SHA3-512)
   │ ⑥ 耐量子電子署名生成 (@noble/post-quantum NIST FIPS 204 ML-DSA-65)
   │ ⑦ POST /api/documents/init (Idempotency-Key付き) で初期化（ステータス: INITIALIZED）
   │ ⑧ R2プリサインドURLへ暗号化PDFバイナリを直接格納
   │ ⑨ キーストア (.riffkey) のダウンロード（Safari ITP対策・未完了ならURL発行不可）
   │    ※ Argon2id（デスクトップ: 64MB / モバイル: 32MBフォールバック）
   │ ⑩ 甲の本人確認：WebAuthn (Touch ID / Face ID) で端末署名バインド
   │    ※ challenge = HMAC-SHA-256(server_random_nonce, original_hash || ml_dsa_public_key)
   ▼
[Vercel (Next.js API - Node.js Runtime)]
   │ ⑪ 原本ハッシュ・甲のML-DSA公開鍵・WebAuthn証跡・当事者メタデータをSupabaseへ登録（ステータス: PENDING）
   │ ⑫ ワンタイム認証トークン生成（SHA-256ハッシュ化保存。平文はレスポンスのみで返却）
   ▼
[甲 (作成者ブラウザ)]
   │ ⑬ ゼロ知識署名URLを発行: https://app.example.com/sign/{token}#{encryption_key}
   │    ※ URL脱落防止のため「完全URL一括コピー」および「手動キー入力フォールバック」を提供
   │    ※ UI常設ガイダンス：「機密文書ではSignal等の暗号化メッセンジャーでのURL共有を推奨します」
   │ ⑭ 甲がSlack / Teams / メール等で乙へ直接共有（サーバーには鍵が一切渡らない）
   ▼
[乙 (署名者ブラウザ)]
   │ ⑮ GET /api/sign/{token}/info で文書メタ情報（原本ハッシュ、作成者公開鍵等）のみ取得
   │    ※ この段階では暗号化PDFのダウンロードURLは未返却（未認証ダウンロードを防止）
   │ ⑯ メールOTP所持認証（電子署名法準拠の身元紐付け）成功により signer_session_token (3時間有効) を取得
   │ ⑰ R2プリサインドダウンロードURLを取得し、暗号化PDFをダウンロード
   │ ⑱ URLのハッシュフラグメント(#)または手動入力から復号鍵を取得し、暗号化PDFを復号
   │ ⑲ 【原本完全性Assertion】復号バイナリのSHA-256をWeb Worker内で即座に再計算し、原本ハッシュと一致を検証
   │ ⑳ 契約書PDFをインメモリ描画・閲覧。甲が設定した事前入力情報（会社名・氏名・住所・肩書等）を確認・入力
   │
   ├── [拒否時] 拒否理由を入力し送信 ──► VercelがREJECTEDログをMerkle Treeに刻印し甲へ通知
   │
   └── [合意時] WebAuthn (Touch ID / Face ID / セキュリティキー) 実行
               ※ challenge = HMAC-SHA-256(server_random_nonce, merkle_latest_node_hash) として渡す
               ※ navigator.credentials.create() でハードウェアキー生成＋アテステーション取得
   ▼
[Vercel (バックエンド締結エンジン - 同期フェーズ)]
   │ ㉑ 乙の認証アテステーションの暗号学的検証 ＆ challenge内のnonceをDB照合
   │ ㉒ Merkle Tree 監査チェーンの「最終ルートハッシュ（Final Merkle Root）」を算出
   │ ㉓ ドキュメントステータスを CONSENT_RECEIVED に遷移
   │ ㉔ 乙へ即座にレスポンス返却：「署名を受理しました。証明書を準備中です」
   ▼
[Vercel (非同期キュー - Background Functions / Inngest - 東京リージョン hnd1)]
   │ ㉕ 世界原子時計（NICT HTTPS API → Google Time → Cloudflare Date フォールバック）時刻取得 → TIMESTAMP_ACQUIRED
   │ ㉖ OpenTimestamps (カレンダー登録) ＆ GitHub API (公開補助アンカー) に最終ルートハッシュを刻印 → ANCHORED
   │ ㉗ 「合意締結証明書（Audit Certificate）PDF」を独立生成 (pdf-lib) してR2へ格納 → CERTIFICATE_GENERATED
   │ ㉘ ステータスを COMPLETED に遷移
   │ ㉙ documents_realtime_status テーブルへ最小限ステータス変更を通知
   ▼
[完了処理・納品]
   ├─► 乙の画面：documents_realtime_status 購読 or ポーリングで完了を検知し
   │            「原本PDF ＋ 合意締結証明書PDF」をZIP一括ダウンロード
   │            （JSZipによるブラウザ内即時生成、大容量時はストリーミング方式でメモリ消費を抑制）
   │            復号鍵を乙のIndexedDBに安全キャッシュ
   └─► 甲・乙へメール送信：誰でも検証可能な「合意締結証明書PDF」のみ直接添付
                           ※原本は手元端末または共有URLから取得する案内文を記載
   ▼
[非同期バックグラウンドバッチ (Vercel Cron / GitHub Actions)]
   ├─► 数時間後にOpenTimestampsの確定ブロックをポーリングし、確定証明書 (.ots) へ自動アップグレード
   └─► INITIALIZED状態のまま30分超過した孤立ドキュメントを自動削除
```

### ② 締結エンジン・ステートマシン

```text
INITIALIZED ──(アップロード&activate)──► PENDING ──► CONSENT_RECEIVED ──► TIMESTAMP_ACQUIRED ──► ANCHORED ──► CERTIFICATE_GENERATED ──► COMPLETED
    │                                      │                │                    │                  │                │
    └──(30分未完了クリーンアップ)            └── EXPIRED      └──(リトライ)        └──(リトライ)       └──(リトライ)     └──(リトライ)
                                           └── CANCELLED                                                        └── ADMIN_REVIEW

※ 各非同期ステップは最大3回まで自動リトライ（指数バックオフ: 10s → 30s → 90s）。
※ 3回超過時は ADMIN_REVIEW へ遷移し、運営者へSlack/メール通知。
※ 乙への画面表示: 「証明書を準備中です（通常1〜2分以内に完了します）」
※ 5分以内に完了しない場合:「完了次第メールでお知らせします」と表示を切り替え。
```

---

## 2. 契約書作成・組版エンジン（Markdown Contract & Signature Fusion）

既存PDFのアップロードに加え、ブラウザ完結で契約書を作成できる「Markdown契約書エディタ」と、原本PDF末尾に法的合意ブロックを付与する「署名合意シート融合エンジン」を搭載しています。

### ① 高精度ブラウザ内A4組版エンジン (`lib/markdown/contract-pdf.ts`)
*   **WebプレビューとPDFの完全一致：**
    *   フォントサイズ（9.5pt）、行送り（16.5pt）、文字色（Slate-800 `#1e293b`）、余白（48pt）をプレビューコンポーネントと完全に同期。
    *   H1見出し（中央揃え、下部境界線）、H2見出し（条文タイトル、ブルーアクセントバー付き）、H3見出し、箇条書き、引用ブロック（左アクセントライン）の完全再現。
*   **日本語禁則処理（Kinsoku Shori）：**
    *   行頭禁則文字（`、。，．）〕］｝〉》」』】ー～?!！？%:;)`）の自動回り込み処理。
    *   フォントメトリクスに基づき `font.widthOfTextAtSize()` で正確に文字幅を計測して折り返しを計算。
*   **ページ跨ぎ制御（Keep-with-Next）と白紙除去：**
    *   見出し（H2/H3）の直後に改ページが入って孤立（Orphan）するのを防ぐため、見出し＋最低2〜3行分の本文スペースを事前確保する `ensureSpace()` ロジック。
    *   コンテンツ末尾の改行により空ページが発生した場合、生成完了前に最後の白紙ページを自動検出して除去（`pdfDoc.removePage`）。

### ② 署名合意シート融合エンジン (`lib/crypto/signature-sheet.ts`)
*   **自動余白判定と追加ページ制御：**
    *   原本PDFの最終ページ下部余白をスキャン。余白が十分（200pt以上）ある場合は最終ページ下部にインライン融合。
    *   余白が不足している場合は、新ページ（A4）を自動追加してフルページ合意シートをレンダリング。
*   **スマートな締結日・当事者情報レンダリング：**
    *   **締結前（未締結時）：** 締結日欄には「締結時に自動確定」と表示し、フライング印字を防止。
    *   **締結後：** NICT原子時計または合意タイムスタンプに基づく正式日時（例: `2026年09月16日`）を確定印字。
    *   **不要項目の非表示制御：** 甲が設定していない項目（例: 会社名や肩書がない個人契約）は行ごと自動非表示とし、無駄な空白や不要項目を排除。
    *   **必須バリデーション：** 契約の法的真正性を担保するため、署名者（乙）の「住所」を必須項目として厳格バリデーション。

---

## 3. 日本語旧字体・異体字対応およびサロゲートペア正規化仕様

公的文書・法人契約において頻出する「人名用漢字」「旧字体」「異体字」を正しく取り扱うため、フォントシステムおよびテキスト正規化エンジンを完全に統合しています。

### ① JIS第1〜第4水準・人名用漢字対応フォント（IPAexゴシック）
*   一般的な常用漢字サブセット（2MB程度）やVariableフォント（可変フォント）をPDFライブラリ（`pdf-lib` + `@pdf-lib/fontkit`）でサブセット化すると、グリフの欠落やASCIIコードへの化けが発生します。
*   本システムでは、静的TrueType形式（TTF）であり、JIS第1〜第4水準および人名用漢字を網羅した公式フォント **`ipaexg.ttf`（IPAexゴシック）** をサーバーおよびクライアント配信API（`/api/fonts/japanese`）に標準搭載しています。
*   これにより、以下の旧字・異体字が**そのまま本来の旧字体グリフで鮮明にPDFへ印字**されます：
    *   「髙」（はしごだか / U+9AD9）
    *   「﨑」（たつさき / U+FA11）
    *   「塚」（つか / U+FA10）
    *   「德」（とく / U+5FB7）
    *   「賴」（らい / U+8CF4）
    *   「邊」「邉」（なべ / U+908A, U+9089）
    *   「齊」「齋」「櫻」「榮」 など

### ② サロゲートペア（「𠮷」等）安全正規化エンジン (`sanitizeTextForPdf`)
*   **PDFサブセッターの仕様制約：**  
    「𠮷（つちよし / U+20BB7）」などのUnicode第2プレーン（SIP領域）の文字は、PDF標準のフォントサブセッター（Format 4 CMap / 16ビットBMP）でエンコードできず、グリフ未検出となって ` 田悠人` のように文字欠落・空白化が発生します。
*   **二重整合性アーキテクチャ：**
    *   **画面（Web UI）、データベース、WebAuthn認証データ、および耐量子署名（ML-DSA）原本データ：**  
        ユーザーが入力した本来の「**𠮷田悠人**」を1ビットの改変もなく完全保持します。
    *   **PDFレンダリングストリーム（本文・合意シート・証明書）：**  
        文字抜けによる契約破損を防止するため、法務・行政フォールバック標準に準拠した正規化エンジン（`sanitizeTextForPdf`）を通して「**吉田悠人**」として安全・確実に印字します。

```typescript
// 主なサロゲートペア文字の安全マッピング定義 (lib/crypto/signature-sheet.ts)
export function sanitizeTextForPdf(text: string): string {
  if (!text) return "";
  return text
    .replace(/\uD842\uDFB7|𠮷/g, "吉") // つちよし (U+20BB7) -> 吉
    .replace(/𡈽/g, "土") // つち (U+2123D)
    .replace(/𡚴/g, "岡") // おか (U+216B4)
    .replace(/𥔎/g, "崎") // さき (U+2550E)
    .replace(/𪚲/g, "亀") // かめ (U+2AA72)
    .replace(/𠔱/g, "免") // めん (U+20531)
    .replace(/𡛀/g, "妻") // つま (U+216C0)
    .replace(/𡿨/g, "川") // かわ (U+21FE8)
    .replace(/𡏄/g, "嶋") // しま (U+213C4)
    .replace(/𡘲/g, "奥") // おく (U+21632)
    .replace(/𢛳/g, "恵") // めぐみ (U+226F3)
    .replace(/𣘺/g, "橋") // はし (U+2363A)
    .replace(/𣜿/g, "樋") // ひ (U+2373F)
    .replace(/𤘩/g, "角") // つの (U+24629)
    .replace(/𥧄/g, "穀") // こく (U+259C4)
    .replace(/𦚰/g, "脇") // わき (U+266B0)
    .replace(/𦝏/g, "脩") // おさむ (U+2674F)
    .replace(/𧦣/g, "誠") // まこと (U+279A3)
    .replace(/𨤒/g, "舘") // たて (U+28912)
    .replace(/𨥆/g, "鐡") // てつ (U+28946)
    .replace(/𩸽/g, "ほっけ")
    .replace(/𪚥/g, "龍")
    .replace(/[\u200B-\u200D\uFEFF]/g, ""); // 不可視制御文字の除去
}
```

---

## 4. データベース設計（Supabase / PostgreSQL）

平文の契約書本文はサーバー・データベースに一切保存せず、**暗号学的証跡・ハッシュツリー・認証メタデータ**のみを保持します。

### ① `documents`（文書メタデータ）
*   `id`: UUID (Primary Key)
*   `encrypted_original_path`: TEXT (Cloudflare R2 / Supabase Storage内の暗号化原本PDFパス)
*   `certificate_pdf_path`: TEXT (合意締結証明書PDFパス。未暗号化で公開可能)
*   `original_sha256`: TEXT (原本PDFのSHA-256ハッシュ)
*   `original_sha3_512`: TEXT (原本PDFのSHA3-512ハッシュ)
*   `file_size_bytes`: BIGINT (暗号化原本のファイルサイズ)
*   `final_merkle_root`: TEXT (全ログ・甲乙生体署名を統合した最終ルートハッシュ)
*   `creator_ml_dsa_public_key`: BYTEA (甲のML-DSA-65耐量子公開鍵: 1,952 bytes)
*   `creator_ml_dsa_public_key_hash`: TEXT (甲のML-DSA公開鍵のSHA-256ハッシュ。一覧検索用)
*   `creator_ml_dsa_signature`: BYTEA (原本SHA3-512に対する甲のML-DSA-65署名: 3,309 bytes)
*   `creator_webauthn_binding`: JSONB (甲のWebAuthnクレデンシャル情報、氏名、住所、組織名、役職)
*   `upload_confirmed`: BOOLEAN DEFAULT false (ストレージ暗号化バイナリ格納完了フラグ)
*   `status`: TEXT (`initialized` | `pending` | `consent_received` | `timestamp_acquired` | `anchored` | `certificate_generated` | `completed` | `rejected` | `expired` | `cancelled` | `admin_review`)
*   `rejection_reason`: TEXT (拒否時の理由コメント)
*   `cancellation_reason`: TEXT (甲によるキャンセル理由)
*   `ots_proof_path`: TEXT (OpenTimestampsの確定証明データ `.ots` のストレージパス)
*   `ots_status`: TEXT (`pending` | `upgraded`)
*   `github_commit_sha`: TEXT (GitHubコミットハッシュ)
*   `retry_count`: INTEGER DEFAULT 0 (非同期処理のリトライ回数)
*   `expires_at`: TIMESTAMPTZ (署名URLの有効期限。デフォルト: 作成から7日)
*   `created_at`: TIMESTAMPTZ
*   `updated_at`: TIMESTAMPTZ (トリガーで自動更新)

### ② `signers`（署名者・認証情報）
*   `id`: UUID (Primary Key)
*   `document_id`: UUID (FK -> documents.id)
*   `signing_order`: INTEGER (署名順序: 3者間以上のマルチ署名対応)
*   `role`: TEXT (`creator` | `signer` | `witness` | `approver`)
*   `email`: TEXT (署名者の連絡先メールアドレス)
*   `identity_auth_type`: TEXT (`email_otp` | `sms_otp` | `oauth_google`)
*   `identity_auth_verified_at`: TIMESTAMPTZ (本人性確認が完了した時刻)
*   `auth_token_hash`: TEXT (ワンタイム認証トークンの**SHA-256ハッシュ**。平文は保存しない)
*   `token_expires_at`: TIMESTAMPTZ (ワンタイムトークンの有効期限)
*   `auth_attempts`: INTEGER DEFAULT 0 (OTP/認証の連続失敗回数。5回超過でロック)
*   `auth_locked_until`: TIMESTAMPTZ (ロック解除時刻。ロック時は15分後に設定)
*   `webauthn_credential_id`: TEXT (端末セキュアエレメントで生成されたクレデンシャルID)
*   `webauthn_public_key`: TEXT (生成されたWebAuthn公開鍵: COSE形式)
*   `webauthn_challenge_nonce`: TEXT (サーバー生成ランダムnonce。検証完了後に消去)
*   `attestation_object`: BYTEA (初回生成時の生体認証アテステーションデータ)
*   `client_data_json`: TEXT (challengeが含まれるclientDataJSON)
*   `auth_level`: TEXT (`high_webauthn` | `medium_security_key` | `low_fallback`)
*   `signed_at`: TIMESTAMPTZ

### ③ `audit_logs`（Merkle Tree 監査チェーン）
*   `id`: BIGSERIAL (Primary Key)
*   `document_id`: UUID (FK -> documents.id)
*   `event_type`: TEXT (`CREATED` | `IDENTITY_VERIFIED` | `VIEWED` | `INTEGRITY_VERIFIED` | `WEBAUTHN_CONSENT` | `REJECTED` | `CANCELLED` | `OTS_ANCHORED` | `OTS_UPGRADED` | `COMPLETED`)
*   `payload_json`: JSONB (IPアドレス, UA, 端末テレメトリ, NICT時刻, 署名者氏名・会社・住所等の当事者メタデータ)
*   `previous_log_hash`: TEXT (1つ前のログノードのハッシュ値。**※ 初回CREATEDイベント時はジェネシスハッシュ `SHA3-512("GENESIS:" || document_id)` を使用**)
*   `current_log_hash`: TEXT (本レコードのSHA3-512ハッシュ値)
*   `created_at`: TIMESTAMPTZ

### ④ `documents_realtime_status`（Realtime購読用安全テーブル）
RLSポリシーによるデータ漏洩を防ぐため、全メタデータを持つ `documents` テーブルを `anon` に直接購読させず、ステータス変更通知専用の最小テーブルを新設。
*   `id`: BIGSERIAL (Primary Key)
*   `document_id`: UUID (FK -> documents.id, UNIQUE)
*   `status`: TEXT (現在のステータス)
*   `certificate_ready`: BOOLEAN DEFAULT false
*   `updated_at`: TIMESTAMPTZ

### ⑤ `idempotency_keys`（API多重リクエスト防止）
*   `key`: TEXT (Primary Key, クライアント提示のUUIDv4)
*   `endpoint`: TEXT (対象エンドポイントパス)
*   `response_status`: INTEGER (キャッシュされたHTTPステータス)
*   `response_body`: JSONB (キャッシュされたレスポンスJSON)
*   `created_at`: TIMESTAMPTZ DEFAULT NOW() (30分TTLで定期クリーンアップ)

---

## 5. 暗号・セキュリティ仕様（NIST FIPS 204 ML-DSA & WebAuthn）

### ① クライアントサイドE2EE（AES-256-GCM）
*   ファイル選択時またはMarkdownからのPDF生成直後、ブラウザ側でマジックバイト（`%PDF-`）を検証。
*   `crypto.subtle.generateKey` により一時対称鍵 `K`（AES-256-GCM, 256bit）を生成。
*   原本PDFを暗号化（IVは12バイト暗号学的乱数）。ストレージへは暗号化バイナリ（IV + Ciphertext + Tag）のみ直接送信。

### ② 復号鍵のゼロ知識受け渡し（Shared Link方式 ＋ 脱落フォールバック）
*   署名URL構造：`https://app.example.com/sign/{token}#{key_base64url}`
*   URLの `#` 以降（ハッシュフラグメント）はRFC準拠で**ブラウザからサーバーへ送信されない**。
*   **フラグメント脱落対策：** チャットツール等で `#key` が欠落して乙に届いた場合に備え、署名画面に「暗号鍵の手動貼り付けフォーム」を用意。

### ③ 甲の耐量子アイデンティティ（NIST FIPS 204 ML-DSA-65）＆ キーストア
*   **ML-DSA-65（Dilithium）格子暗号：**  
    将来の量子コンピュータ（Shorのアルゴリズム）による暗号解読に対抗するため、甲の原本署名には最新の耐量子標準 NIST FIPS 204 ML-DSA-65（公開鍵: 1,952 bytes, 署名: 3,309 bytes）を採用。
*   **Argon2id キーストア (`.riffkey`)：**
    *   Safari ITPによるIndexedDB自動パージ対策として、パスフレーズ保護された `.riffkey` のダウンロードを必須化。
    *   デスクトップ（64MB memory, iterations 3）/ モバイル（32MB memory, iterations 6）の動的適応。

### ④ WebAuthn challenge のサーバーnonce結合方式（HMACバインド）
*   リプレイ攻撃およびハッシュ伸長攻撃（Length Extension Attack）を排除するため、HMAC-SHA-256を採用：
    ```
    // 甲（作成者）のWebAuthn challenge:
    challenge = HMAC-SHA-256(
      key: server_random_nonce_32bytes,
      data: original_sha256 || ml_dsa_public_key
    )

    // 乙（署名者）のWebAuthn challenge:
    challenge = HMAC-SHA-256(
      key: server_random_nonce_32bytes,
      data: merkle_latest_node_hash
    )
    ```

---

## 6. トリプル外部アンカー・タイムスタンプ仕様

単一の機関やサーバーに依存せず、独立した3つの地球規模インフラへ最終ルートハッシュを同時刻印します。

```text
               ┌───────────────────────────┐
               │     Final Merkle Root     │
               └─────────────┬─────────────┘
         ┌───────────────────┼───────────────────┐
         ▼                   ▼                   ▼
┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
│ NICT 日本標準時  │ │ OpenTimestamps  │ │ GitHub Witness  │
│  (原子時計UTC)  │ │  (Bitcoin PoW)  │ │ (公開リポジトリ) │
└─────────────────┘ └─────────────────┘ └─────────────────┘
```

1. **NICT（情報通信研究機構）原子時計タイムスタンプ:**
   * 日本標準時（JST/UTC）を管理するNICTのHTTPS API（`https://ntp-a1.nict.go.jp/cgi-bin/json`）と通信し、ミリ秒単位の国家標準時を取得・刻印。
   * Google Time API、Cloudflare Edge Date への自動フォールバック設計。
2. **OpenTimestamps（ビットコイン・ブロックチェーン）：**
   * カレンダーサーバー（OTS Pool）を経由してビットコインのマークルツリーに登録。
   * 定期バッチによりビットコイン確定ブロック情報（`.ots`）へ自動アップグレード。
3. **GitHub 公開証拠リポジトリ（Witness Anchor）：**
   * 公開リポジトリ（`riffaegis-anchors`）のGitコミットとしてドキュメントIDとハッシュを即座に一般公開。

---

## 7. 合意締結証明書（Audit Certificate）仕様・法廷証拠力

原本PDFを変更しない「デタッチド（分離型）監査証明書」をサーバーサイド（`lib/server/certificate.ts`）で自動生成します。

### ① 証明書記載項目
*   **1. Document Core Identity:**
    *   Document ID, Original SHA-256, Original SHA3-512, File Size, Final Merkle Root
*   **2. Creator (Party A) - Identity & Post-Quantum Key:**
    *   Organization / Name（甲の組織名・氏名）
    *   Address（甲の住所）
    *   Creator Key Hash, Algorithm（NIST FIPS 204 ML-DSA-65）, Created Timestamp
*   **3. Signer (Party B) - WebAuthn Biometric Consent:**
    *   Organization / Name（乙の組織名・氏名）
    *   Title / Student ID（乙の役職・肩書）
    *   Signer Address（乙の住所）
    *   Signer Email, Authentication Level (`HIGH_WEBAUTHN` / `MEDIUM` / `LOW`)
    *   WebAuthn Credential ID, Agreement Signed At
*   **4. Distributed External Anchors:**
    *   Atomic Time Source & Timestamp（NICT）
    *   OpenTimestamps Anchor Status（Bitcoin Block）
    *   GitHub Proof Anchor Commit SHA
*   **5. Cryptographic Merkle Tree Audit Chain:**
    *   直近の全監査ログのイベント名、UTCタイムスタンプ、個別ハッシュ値テーブル

### ② メタデータ埋め込み
*   PDFドキュメントプロパティ（Title, Subject, Keywords）に、監査メタデータのJSON文字列（`RIFFAEGIS_AUDIT_DATA:...`）を機械可読形式で完全埋め込み。
*   スタンドアローン検証ポータル（`verify.riff-aegis.com`）にドロップするだけで、オフライン環境でも完全な数学的・暗号学的検証が可能。

---

## 8. APIエンドポイント詳細仕様

### ① ドキュメント作成フロー（甲）
*   `POST /api/documents/init` : メタデータ・耐量子公開鍵登録（Idempotency対応、ストレージアップロードURL返却）
*   `POST /api/documents/:id/confirm-upload` : ストレージ格納確認・サイズ検証
*   `POST /api/documents/:id/webauthn/register-challenge` : 甲のWebAuthn challenge発行
*   `POST /api/documents/:id/webauthn/register-complete` : 甲の生体認証バインド完了
*   `POST /api/documents/:id/activate` : `.riffkey` バックアップ確認後、署名URL発行（ステータス: `pending`）

### ② 署名フロー（乙）
*   `GET /api/sign/:token/info` : 文書基本メタ情報取得（PDFダウンロードURLは返却しない）
*   `POST /api/sign/:token/otp/send` : 乙のメール宛に6桁OTP送信（レート制限: 5回/時）
*   `POST /api/sign/:token/otp/verify` : OTP検証成功時に `signer_session_token` と一時ダウンロードURL発行
*   `GET /api/sign/:token/download-url` : 暗号化PDFダウンロードURLの再取得（15分有効）
*   `POST /api/sign/:token/webauthn/challenge` : Merkle Root結合WebAuthn challenge発行
*   `POST /api/sign/:token/consent` : 生体合意アテステーション検証、当事者メタデータ保存、非同期締結キューディスパッチ
*   `POST /api/sign/:token/reject` : 署名拒否理由の送信・監査ログ刻印

### ③ フォント配信・ステータス・ダウンロード
*   `GET /api/fonts/japanese` : クライアントサイドPDF生成・合意シート用フォント（`ipaexg.ttf`）のストリーミング配信
*   `GET /api/documents/:id/status` : リアルタイムポーリング用ステータス取得
*   `GET /api/documents/:id/certificate` : 合意締結証明書PDFおよび `.ots` のダウンロードURL発行

### ④ 管理・運用（甲）
*   `GET /api/documents` : 甲の作成ドキュメント一覧（JWT内公開鍵ハッシュで絞り込み）
*   `POST /api/documents/:id/cancel` : `pending` ドキュメントの取り消し
*   `POST /api/identity/restore` : `.riffkey` 復号秘密鍵署名による甲のセッション復元
*   `POST /api/identity/rebind-webauthn` : 端末移行時の新WebAuthnクレデンシャル再登録

### ⑤ 定期バッチ（Cron）
*   `GET /api/cron/cleanup` : 30分放置された孤立ドキュメント・有効期限切れレコードのパージ
*   `GET /api/cron/daily` : OpenTimestamps の確定ビットコインブロック（`.ots`）への自動アップグレード

---

## 9. 認可モデル・セッショントークン（JWT）仕様

永続的なユーザーアカウントを持たないため、用途ごとに分離されたJWT階層を採用：

1. **`creator_document_token` (甲・特定ドキュメント操作用)**:
   * 有効期間: 24時間
   * スコープ: 特定ドキュメントのアップロード確認、WebAuthn登録、アクティベーション
2. **`creator_identity_token` (甲・ダッシュボード横断管理用)**:
   * 有効期間: 24時間（`.riffkey` 再認証でいつでも再発行可能）
   * スコープ: 作成ドキュメント一覧閲覧、キャンセル
3. **`signer_session_token` (乙・署名セッション用)**:
   * 有効期間: **3時間**（契約書の精読・検討時間を十分に確保）
   * スコープ: 原本復号ダウンロード、WebAuthn合意・拒否

---

## 10. データリテンション・運用バッチ処理

| データ区分 | 保存期間 | 根拠・処理 |
|-----------|---------|-----------|
| 初期化未確認（`initialized`） | 作成後 **30分** | 中断・切断による孤立レコードをCronで自動パージ |
| 暗号化原本PDF（ストレージ） | 締結完了後 **10年** | 商事債権の消滅時効（民法166条: 10年） |
| 合意締結証明書PDF | **無期限** | 公開検証可能な証拠として永続保存 |
| OTS確定証明書 `.ots` | **無期限** | ビットコインアンカーの数学的検証用 |
| `documents` / `audit_logs` | 締結完了後 **10年** | 原本と同期して保持 |
| `signers` 個人情報 | 締結完了後 **10年** | 期間経過後はハッシュ置換による完全匿名化 |
| キャンセル・失効ドキュメント | ステータス変更後 **90日** | 自動パージ |
| `idempotency_keys` | 作成後 **30分** | 重複排除ウィンドウ経過後に自動パージ |

---

## 11. メール送信基盤

*   **サービス選定:** Resend（Vercel推奨トランザクショナルメール基盤）
*   **送信元アドレス:** `RiffAegis <security@riffaegis.rifflink.com>` (SPF/DKIM/DMARC設定済み)
*   **テンプレート一覧:**
    *   `otp-verification` : 乙への6桁認証コード送信（10分有効）
    *   `signing-completed` : 甲・乙双方へ「合意締結証明書PDF」を直接添付して送付
    *   `signing-rejected` : 乙の拒否理由を甲へ通知
    *   `document-expired` : 署名期限切れを甲へ通知

---

## 12. 環境変数・シークレット管理

| 変数名 | 用途 | 公開範囲 |
|--------|------|---------|
| `NEXT_PUBLIC_SUPABASE_URL` | Supabase API URL | 公開 |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Supabase anonキー（Realtime用） | 公開 |
| `NEXT_PUBLIC_APP_URL` | アプリケーション基底URL | 公開 |
| `SUPABASE_SERVICE_ROLE_KEY` | Supabase管理者サービスロールキー | サーバー秘匿 |
| `STORAGE_BUCKET_NAME` | オブジェクトストレージバケット名（`riff-aegis`） | サーバー秘匿 |
| `JWT_SECRET` | 認可JWT署名秘密鍵（32バイト以上） | サーバー秘匿 |
| `RESEND_API_KEY` | Resend APIトークン | サーバー秘匿 |
| `EMAIL_FROM` | 送信元メールアドレス設定 | サーバー秘匿 |
| `GITHUB_TOKEN` | GitHubアンカーコミット用PAT | サーバー秘匿 |
| `GITHUB_ANCHOR_REPO` | アンカー用リポジトリ名（例: `RiffLink/riffaegis-anchors`） | サーバー秘匿 |
| `CRON_SECRET` | 定期バッチ保護用Bearerトークン | サーバー秘匿 |

---

## 13. 依存パッケージ・テスト検証スイート

### ① サーバーサイド
*   `next`: 15.x App Router
*   `@supabase/supabase-js`: PostgreSQL & Storage
*   `@aws-sdk/client-s3` & `@aws-sdk/s3-request-presigner`: S3互換ストレージ
*   `@simplewebauthn/server`: FIDO2 / WebAuthn アテステーション・アサーション暗号検証
*   `pdf-lib` & `@pdf-lib/fontkit`: 契約書・証明書PDF生成、フォント埋め込み
*   `opentimestamps`: ビットコインブロックチェーン刻印
*   `@octokit/rest`: GitHub APIアンカーコミット
*   `resend`: トランザクショナルメール送信
*   `jose`: JWT署名・検証

### ② クライアントサイド
*   `@noble/post-quantum`: NIST FIPS 204 ML-DSA-65 耐量子署名
*   `@simplewebauthn/browser`: ブラウザ生体認証操作
*   `hash-wasm`: Argon2id 高速WASM実装
*   `pdfjs-dist`: ブラウザ内PDFインメモリレンダリング
*   `jszip`: 原本・証明書一括ダウンロード生成

### ③ 自動テスト検証（Vitest）
全39件のテストケースを標準実装し、全件グリーン（Pass）を継続的に維持：
*   `tests/server.test.ts` (5 tests) : サーバーコアAPI・バリデーション
*   `tests/signature_sheet.test.ts` (3 tests) : 署名合意シート融合・旧字体印字
*   `tests/api_creator.test.ts` (1 test) : 作成者ライフサイクル
*   `tests/api_signer.test.ts` (1 test) : 署名者OTP〜WebAuthn締結フロー
*   `tests/cron.test.ts` (3 tests) : 孤立ドキュメントパージ・OTSアップグレード
*   `tests/markdown_pdf.test.ts` (4 tests) : Markdown組版・白紙除去・マルチページ
*   `tests/security_fixes.test.ts` (6 tests) : RLS・レート制限・CSRF対策
*   `tests/crypto.test.ts` (16 tests) : ML-DSA耐量子署名・Argon2idキーストア