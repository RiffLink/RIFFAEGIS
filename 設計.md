# 完全要塞型・電子署名サービス 基本設計書 (RiffAegis)

## 1. システム全体構成（ゼロ知識・デタッチド監査アーキテクチャ）

本システムは**「通常運用において運営者（サーバー）すら契約書を閲覧できないクライアントサイド暗号化（E2EE）」**と**「地球規模の分散インフラによる多重耐改ざん保証」**を前提とします。
原本PDFを一切変更せず、独立した「合意締結証明書（Audit Certificate）」を発行する**デタッチド（分離型）契約パケット方式**を採用します。

> [!NOTE]
> **ゼロ知識アーキテクチャに関する誠実な定義**  
> Webブラウザ完結型の暗号化サービスであるため、「サーバーが配信するJavaScript自体の改ざんリスク」をゼロにはできません。本設計では**厳格なCSP（Content Security Policy）**による外部通信の完全制限、**SRI（Subresource Integrity）**、および**オープンソース再現可能ビルド（Reproducible Build）**を組み合わせ、運営者やインフラ侵入者によるコード改ざんを極小化・検知可能にする設計をとります。

> [!CAUTION]
> **脅威モデルの前提条件：共有チャネルの安全性**  
> 本システムのE2EEは「サーバー（運営者）に対する」ゼロ知識性を保証するものです。暗号鍵を含む署名URLは甲から乙へSlack / Teams / メール等で直接共有されるため、**共有チャネル自体の傍受リスクには甲の責任で対処する必要があります**。機密性の高い契約では、Signal等のE2Eで暗号化されたメッセンジャーの利用を強く推奨するガイダンスをUIに常設表示します。

### ① 全体シーケンス図

```text
[甲 (作成者ブラウザ)]
   │ ① 原本PDF選択・検証（マジックバイト %PDF- 検査、初期上限20MB）
   │    ＆ クライアントサイド暗号化 (AES-GCM-256)
   │ ② 二重ハッシュ計算 (SHA-256 + SHA3-512)
   │ ③ 耐量子署名生成 (@noble/post-quantum ML-DSA-65)
   │ ④ POST /api/documents/init (Idempotency-Key付き) で初期化（ステータス: INITIALIZED）
   │ ⑤ R2プリサインドURLへ暗号化PDFバイナリを直接格納
   │ ⑥ キーストア (.riffkey) の必須ダウンロード（Safari ITP対策・未完了ならURL発行不可）
   │    ※ Argon2id（デスクトップ: 64MB / モバイル: 32MBフォールバック）
   │ ⑦ 甲の本人確認：WebAuthn (Touch ID / Face ID) で端末署名バインド
   │    ※ challenge = HMAC-SHA-256(server_random_nonce, original_hash || ml_dsa_public_key)
   ▼
[Vercel (Next.js API - Node.js Runtime)]
   │ ⑧ 原本ハッシュ・甲のML-DSA公開鍵・WebAuthn証跡をSupabaseへ登録（ステータス: PENDING）
   │ ⑨ ワンタイム認証トークン生成（SHA-256ハッシュ化保存。平文はレスポンスのみで返却）
   ▼
[甲 (作成者ブラウザ)]
   │ ⑩ ゼロ知識署名URLを発行: https://app.example.com/sign/{token}#{encryption_key}
   │    ※ URL脱落防止のため「完全URL一括コピー」および「手動キー入力フォールバック」を提供
   │    ※ UI常設ガイダンス：「機密文書ではSignal等の暗号化メッセンジャーでのURL共有を推奨します」
   │ ⑪ 甲がSlack / Teams / メール等で乙へ直接共有（サーバーには鍵が一切渡らない）
   ▼
[乙 (署名者ブラウザ)]
   │ ⑫ GET /api/sign/{token}/info で文書メタ情報（原本ハッシュ、作成者公開鍵等）のみ取得
   │    ※ この段階では暗号化PDFのダウンロードURLは未返却（未認証ダウンロードを防止）
   │ ⑬ メールOTP所持認証（電子署名法準拠の身元紐付け）成功により signer_session_token (3時間有効) を取得
   │ ⑭ R2プリサインドダウンロードURLを取得し、暗号化PDFをダウンロード
   │ ⑮ URLのハッシュフラグメント(#)または手動入力から復号鍵を取得し、暗号化PDFを復号
   │ ⑯ 【原本完全性Assertion】復号バイナリのSHA-256をWeb Worker内で即座に再計算し、原本ハッシュと一致を検証
   │ ⑰ 一致確認後、PDFをインメモリ描画・閲覧（Web Workerでオフスレッド処理）
   │ 
   ├── [拒否時] 拒否理由を入力し送信 ──► VercelがREJECTEDログをMerkle Treeに刻印し甲へ通知
   │
   └── [合意時] WebAuthn (Touch ID / Face ID / セキュリティキー) 実行
               ※ challenge = HMAC-SHA-256(server_random_nonce, merkle_latest_node_hash) として渡す
               ※ navigator.credentials.create() でハードウェアキー生成＋アテステーション取得
   ▼
[Vercel (バックエンド締結エンジン - 同期フェーズ)]
   │ ⑱ 乙の認証アテステーションの暗号学的検証 ＆ challenge内のnonceをDB照合
   │ ⑲ Merkle Tree 監査チェーンの「最終ルートハッシュ（Final Merkle Root）」を算出
   │ ⑳ ドキュメントステータスを CONSENT_RECEIVED に遷移
   │ ㉑ 乙へ即座にレスポンス返却：「署名を受理しました。証明書を準備中です」
   ▼
[Vercel (非同期キュー - Background Functions / Inngest - 東京リージョン hnd1)]
   │ ㉒ 世界原子時計（NICT HTTPS API → Google Time → Cloudflare Date フォールバック）時刻取得 → TIMESTAMP_ACQUIRED
   │ ㉓ OpenTimestamps (カレンダー登録) ＆ GitHub API (公開補助アンカー) に最終ルートハッシュを刻印 → ANCHORED
   │ ㉔ 「合意締結証明書（Audit Certificate）PDF」を独立生成 (pdf-lib) してR2へ格納 → CERTIFICATE_GENERATED
   │ ㉕ ステータスを COMPLETED に遷移
   │ ㉖ documents_realtime_status テーブルへ最小限ステータス変更を通知
   ▼
[完了処理・納品]
   ├─► 乙の画面：documents_realtime_status 購読 or ポーリングで完了を検知し
   │            「原本PDF ＋ 合意締結証明書PDF」をZIP一括ダウンロード
   │            （JSZipによるブラウザ内即時生成、大容量時はストリーミング方式でメモリ消費を抑制）
   │            復号鍵を乙のIndexedDBに安全キャッシュ
   └─► 甲・乙へメール送信：誰でも検証可能な「合意締結証明書PDF」のみ直接添付
                           ※原本は手元端末または共有URLから取得する案内文を記載
   ▼
[非同期バックグラウンドバッチ (Vercel Cron / GitHub Actions)]
   ├─► 数時間後にOpenTimestampsの確定ブロックをポーリングし、確定証明書 (.ots) へ自動アップグレード
   └─► INITIALIZED状態のまま30分超過した孤立ドキュメントを自動削除
```

### ② 締結エンジン・ステートマシン（エラー耐性設計）

バックエンド締結処理は以下のステートマシンで管理し、各遷移ステップをidempotent（冪等）に設計します。任意のステップで障害が発生しても、最後に成功したステートからリトライ可能です。

```text
INITIALIZED ──(アップロード&activate)──► PENDING ──► CONSENT_RECEIVED ──► TIMESTAMP_ACQUIRED ──► ANCHORED ──► CERTIFICATE_GENERATED ──► COMPLETED
    │                                      │                │                    │                  │                │
    └──(30分未完了クリーンアップ)            └── EXPIRED      └──(リトライ)        └──(リトライ)       └──(リトライ)     └──(リトライ)
                                           └── CANCELLED                                                        └── ADMIN_REVIEW

※ 各非同期ステップは最大3回まで自動リトライ（指数バックオフ: 10s → 30s → 90s）。
※ 3回超過時は ADMIN_REVIEW へ遷移し、運営者へSlack/メール通知。
※ 乙への画面表示: 「証明書を準備中です（通常1〜2分以内に完了します）」
※ 5分以内に完了しない場合:「完了次第メールでお知らせします」と表示を切り替え。
```

---

## 2. データベース設計（Supabase / PostgreSQL）

サーバーには契約書の平文を一切保存しないため、**メタデータ、ハッシュツリー、暗号学的証跡のみ**を保持します。

### ① `documents`（文書メタデータ）
*   `id`: UUID (Primary Key)
*   `encrypted_original_path`: TEXT (Cloudflare R2内の暗号化原本PDFパス)
*   `certificate_pdf_path`: TEXT (Cloudflare R2内の合意締結証明書PDFパス。未暗号化で公開可能)
*   `original_sha256`: TEXT (原本PDFのSHA-256ハッシュ)
*   `original_sha3_512`: TEXT (原本PDFのSHA3-512ハッシュ)
*   `file_size_bytes`: BIGINT (暗号化原本のファイルサイズ)
*   `final_merkle_root`: TEXT (全ログ・甲乙生体署名を統合した最終ルートハッシュ)
*   `creator_ml_dsa_public_key`: BYTEA (甲のML-DSA-65耐量子公開鍵: 1,952 bytes)
*   `creator_ml_dsa_public_key_hash`: TEXT (甲のML-DSA公開鍵のSHA-256ハッシュ。一覧検索用)
*   `creator_ml_dsa_signature`: BYTEA (原本SHA3-512に対する甲のML-DSA-65署名: 3,309 bytes)
*   `creator_webauthn_binding`: JSONB (甲のWebAuthnクレデンシャルID・公開鍵・バインド署名)
*   `upload_confirmed`: BOOLEAN DEFAULT false (R2暗号化バイナリ格納完了フラグ)
*   `status`: TEXT (`initialized` | `pending` | `consent_received` | `timestamp_acquired` | `anchored` | `certificate_generated` | `completed` | `rejected` | `expired` | `cancelled` | `admin_review`)
*   `rejection_reason`: TEXT (拒否時の理由コメント)
*   `cancellation_reason`: TEXT (甲によるキャンセル理由)
*   `ots_proof_path`: TEXT (OpenTimestampsの確定証明データ `.ots` のR2パス)
*   `ots_status`: TEXT (`pending` | `upgraded`)
*   `github_commit_sha`: TEXT (GitHubコミットハッシュ)
*   `retry_count`: INTEGER DEFAULT 0 (非同期処理のリトライ回数)
*   `expires_at`: TIMESTAMPTZ (署名URLの有効期限。デフォルト: 作成から7日)
*   `created_at`: TIMESTAMPTZ
*   `updated_at`: TIMESTAMPTZ (トリガーで自動更新)

### ② `signers`（署名者・認証情報）
*   `id`: UUID (Primary Key)
*   `document_id`: UUID (FK -> documents.id)
*   `signing_order`: INTEGER (署名順序: 3者間以上のマルチ署名対応)
*   `role`: TEXT (`creator` | `signer` | `witness` | `approver`)
*   `email`: TEXT (署名者の連絡先メールアドレス)
*   `identity_auth_type`: TEXT (`email_otp` | `sms_otp` | `oauth_google`)
*   `identity_auth_verified_at`: TIMESTAMPTZ (本人性確認が完了した時刻)
*   `auth_token_hash`: TEXT (ワンタイム認証トークンの**SHA-256ハッシュ**。平文は保存しない)
*   `token_expires_at`: TIMESTAMPTZ (ワンタイムトークンの有効期限)
*   `auth_attempts`: INTEGER DEFAULT 0 (OTP/認証の連続失敗回数。5回超過でロック)
*   `auth_locked_until`: TIMESTAMPTZ (ロック解除時刻。ロック時は15分後に設定)
*   `webauthn_credential_id`: TEXT (端末セキュアエレメントで生成されたクレデンシャルID)
*   `webauthn_public_key`: TEXT (生成されたWebAuthn公開鍵: COSE形式)
*   `webauthn_challenge_nonce`: TEXT (サーバー生成ランダムnonce。検証完了後に消去)
*   `attestation_object`: BYTEA (初回生成時の生体認証アテステーションデータ)
*   `client_data_json`: TEXT (challengeが含まれるclientDataJSON)
*   `auth_level`: TEXT (`high_webauthn` | `medium_security_key` | `low_fallback`)
*   `signed_at`: TIMESTAMPTZ

### ③ `audit_logs`（Merkle Tree 監査チェーン）
*   `id`: BIGSERIAL (Primary Key)
*   `document_id`: UUID (FK -> documents.id)
*   `event_type`: TEXT (`CREATED` | `IDENTITY_VERIFIED` | `VIEWED` | `INTEGRITY_VERIFIED` | `WEBAUTHN_CONSENT` | `REJECTED` | `CANCELLED` | `OTS_ANCHORED` | `OTS_UPGRADED` | `COMPLETED`)
*   `payload_json`: JSONB (IPアドレス, UA, 端末テレメトリ, NICT HTTPS時刻, 認証ログ)
*   `previous_log_hash`: TEXT (1つ前のログノードのハッシュ値。**※ 初回CREATEDイベント時はジェネシスハッシュ `SHA3-512("GENESIS:" || document_id)` を使用**)
*   `current_log_hash`: TEXT (本レコードのSHA3-512ハッシュ値)
*   `created_at`: TIMESTAMPTZ

### ④ `documents_realtime_status`（Realtime購読用安全テーブル）
RLSポリシーによるデータ漏洩（Critical 1）を防ぐため、全メタデータを持つ `documents` テーブルを `anon` に直接購読させず、ステータス変更通知専用の最小テーブルを新設します。
*   `id`: BIGSERIAL (Primary Key)
*   `document_id`: UUID (FK -> documents.id, UNIQUE)
*   `status`: TEXT (現在のステータス)
*   `certificate_ready`: BOOLEAN DEFAULT false
*   `updated_at`: TIMESTAMPTZ

### ⑤ `idempotency_keys`（API多重リクエスト防止）
*   `key`: TEXT (Primary Key, クライアント提示のUUIDv4)
*   `endpoint`: TEXT (対象エンドポイントパス)
*   `response_status`: INTEGER (キャッシュされたHTTPステータス)
*   `response_body`: JSONB (キャッシュされたレスポンスJSON)
*   `created_at`: TIMESTAMPTZ DEFAULT NOW() (30分TTLで定期クリーンアップ)

### ⑥ インデックス・ロック・トリガー戦略

```sql
-- パフォーマンスインデックス
CREATE INDEX idx_documents_status ON documents(status);
CREATE INDEX idx_documents_expires_at ON documents(expires_at) WHERE status = 'pending';
CREATE INDEX idx_documents_creator_key_hash ON documents(creator_ml_dsa_public_key_hash);
CREATE INDEX idx_signers_document_id ON signers(document_id);
CREATE INDEX idx_signers_email ON signers(email);
CREATE INDEX idx_audit_logs_document_id ON audit_logs(document_id);
CREATE INDEX idx_audit_logs_event_type ON audit_logs(document_id, event_type);
CREATE INDEX idx_idempotency_created_at ON idempotency_keys(created_at);

-- Merkle Tree書き込み時の排他制御
-- 同一documentに対する監査ログ追加はAdvisory Lockで直列化する
-- INSERT INTO audit_logs 実行前に:
--   SELECT pg_advisory_xact_lock(hashtext(document_id::text));
-- これにより previous_log_hash → current_log_hash チェーンの整合性を保証する

-- updated_at 自動更新トリガー
CREATE OR REPLACE FUNCTION update_updated_at()
RETURNS TRIGGER AS $$
BEGIN
  NEW.updated_at = NOW();
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER documents_updated_at
  BEFORE UPDATE ON documents
  FOR EACH ROW EXECUTE FUNCTION update_updated_at();

-- documents_realtime_status への自動同期トリガー
CREATE OR REPLACE FUNCTION sync_documents_realtime_status()
RETURNS TRIGGER AS $$
BEGIN
  INSERT INTO documents_realtime_status (document_id, status, certificate_ready, updated_at)
  VALUES (
    NEW.id,
    NEW.status,
    (NEW.certificate_pdf_path IS NOT NULL),
    NEW.updated_at
  )
  ON CONFLICT (document_id) DO UPDATE SET
    status = EXCLUDED.status,
    certificate_ready = EXCLUDED.certificate_ready,
    updated_at = EXCLUDED.updated_at;
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER trigger_sync_documents_realtime_status
  AFTER INSERT OR UPDATE OF status, certificate_pdf_path ON documents
  FOR EACH ROW EXECUTE FUNCTION sync_documents_realtime_status();
```

---

## 3. 暗号・セキュリティ仕様

### ① クライアントサイドE2EE（ゼロ知識暗号化）とCSP制限
*   **ファイル検証：** ファイル選択時、ブラウザ側で先頭4バイトのマジックバイト（`%PDF-` / `0x25 0x50 0x44 0x46`）を検証。不正な拡張子・ファイル形式を暗号化前に遮断。
*   甲のブラウザ内で `crypto.subtle.generateKey` により一時対称鍵 `K`（AES-256-GCM, 256bit）を生成。
*   原本PDFを暗号化（IVは12バイト乱数）。R2へは暗号化バイナリ（IV + Ciphertext + Tag）のみ送信。
*   ファイルサイズ上限は**初期20MB**に設定（モバイルSafariのクラッシュ対策）。大容量ファイルはWeb Workerでストリーミング暗号化し、メインスレッドのブロックを回避する。
*   **厳格なContent Security Policy（CSP）：**
    ```http
    Content-Security-Policy:
      default-src 'self';
      script-src 'self';
      connect-src 'self'
        https://YOUR_PROJECT_ID.supabase.co
        https://YOUR_BUCKET.r2.cloudflarestorage.com
        https://ntp-a1.nict.go.jp
        https://api.github.com
        https://a.pool.opentimestamps.org
        https://b.pool.opentimestamps.org;
      img-src 'self' data: blob:;
      frame-ancestors 'none';
      object-src 'none';
      base-uri 'self';
      form-action 'self';
    ```
    * `connect-src` は自プロジェクトのSupabase/R2 URLに厳密限定（ワイルドカード禁止）。
    * `frame-ancestors 'none'` でクリックジャッキングを防止。
    * `object-src 'none'` でFlash/プラグイン経由の攻撃を防止。
    * `base-uri 'self'` で `<base>` タグハイジャックを防止。
    * `form-action 'self'` でフォーム送信先の乗っ取りを防止。
    * 許可されていない外部ドメインへの接続をブラウザレベルで遮断し、万一のスクリプト混入時にも平文や鍵の外部送信を防止する。

### ② 復号鍵のゼロ知識受け渡し（Shared Link方式 ＋ 脱落フォールバック）
*   署名URLの構造：`https://app.example.com/sign/{token}#{key_base64url}`
*   URLの `#` 以降（ハッシュフラグメント）はRFC準拠で**ブラウザからサーバーへ送信されない**。
*   **フラグメント脱落対策：** チャットツール等で `#key` が欠落して乙に届いた場合に備え、署名画面に「暗号鍵の手動貼り付けフォーム」を用意。甲の画面には「URL＋鍵の一括コピー」ボタンと「鍵単体コピー」ボタンを両方備える。
*   **共有チャネルに関するUIガイダンス：** URLコピー画面に「🔒 機密性の高い契約では、Signal等の暗号化メッセンジャーでの共有を推奨します」を常設表示。メールで共有する場合のリスク説明をツールチップで提供。

### ③ 甲の耐量子アイデンティティ ＆ Safari ITP対策
*   甲のML-DSA-65キーペアはブラウザの `IndexedDB` に永続化し、`navigator.storage.persist()` をリクエスト。`persist()` が拒否された場合もキーストアの必須ダウンロード（下記）により対策済みとする。
*   **キーストア保存の強制化（必須UI）：**
    *   SafariのITP（7日間非操作でのIndexedDB自動パージ）によるアイデンティティ消滅を防ぐため、甲が設定したパスフレーズで暗号化した **`.riffkey` ファイルのダウンロードを必須ステップ** とする。
    *   **暗号化パラメータ（Argon2id）と端末適応：**
        *   **デスクトップ：** `memory: 64MB, iterations: 3, parallelism: 4`
        *   **モバイル / 低メモリ環境：** `memory: 32MB, iterations: 6, parallelism: 1`（WASMメモリ確保失敗を回避）
        *   `.riffkey` のヘッダー（メタデータJSON）に使用パラメータ（memory, iterations, parallelism, salt, iv）を記録し、復元時に動的適用。
    *   **端末移行時のWebAuthn再登録仕様：**
        *   WebAuthnの秘密鍵は端末のセキュアエレメント（Secure Enclave / TPM）に物理的にバインドされているため、ファイル等で新端末へ移行することは技術的に不可能です。
        *   したがって、`.riffkey` で復元・移行されるのは**甲のML-DSA耐量子鍵ペアのみ**です。
        *   新端末移行時は、`.riffkey` 復号後に**新端末のWebAuthn認証器を `create()` で新規再登録**します。サーバー側は甲のML-DSA秘密鍵による署名検証をもって新クレデンシャルの登録・リンクを認可します。
    *   キーストアのバックアップが完了するまで、署名用URLの発行ボタンを非活性（ロック）にする。

> [!CAUTION]
> **鍵の完全消失に関する不可逆リスク（設計上の意図的トレードオフ）**  
> 本システムのゼロ知識設計上、運営者は復号鍵を一切保持しません。したがって：
> * 甲が `.riffkey` を紛失し、かつ IndexedDB がパージされた場合、**甲のML-DSA秘密鍵は永久に復元不可能**です。
> * 甲・乙の両方がAES復号鍵（URLフラグメント）を紛失した場合、**R2上の暗号化原本は永久に復号不可能**です。
>
> これはゼロ知識アーキテクチャの本質的帰結であり、意図的なトレードオフです。UIには以下の警告を常設します：
> * 初回利用時：「⚠️ 鍵ファイル (.riffkey) を紛失すると、あなたの署名者IDは二度と復元できません」
> * URL発行時：「⚠️ この暗号鍵を紛失すると、原本文書は二度と取得できません。必ず安全な場所に保管してください」

*   甲のアップロード時に甲自身の WebAuthn（Touch ID / Face ID）を実行し、**「原本ハッシュ ＋ 甲のML-DSA公開鍵」**を端末セキュアエレメントで署名バインド。

### ④ WebAuthn challenge のサーバーnonce結合方式（HMAC仕様準拠バインド）
*   WebAuthn仕様ではchallengeはサーバー生成のランダムnonceであることが求められる。本システムでは**ドキュメントバインド**、**リプレイ攻撃防止**、および**Length Extension攻撃への完全耐性**を両立するため、HMAC-SHA-256を採用する：
    ```
    // 甲（作成者）のWebAuthn challenge:
    challenge = HMAC-SHA-256(
      key: server_random_nonce_32bytes,
      data: original_sha256 || ml_dsa_public_key
    )

    // 乙（署名者）のWebAuthn challenge:
    challenge = HMAC-SHA-256(
      key: server_random_nonce_32bytes,
      data: merkle_latest_node_hash
    )
    ```
*   `server_random_nonce` はリクエストごとにサーバーで `crypto.randomBytes(32)` で生成し、`signers.webauthn_challenge_nonce` に一時保存。
*   検証時にサーバーは保存済みnonceと対象ハッシュから期待されるHMACを再計算し、`clientDataJSON.challenge` と照合。一致確認後にnonce行を消去。
*   HMAC構造によりMerkle-Damgård型ハッシュ関数の伸長攻撃（Length Extension Attack）を原理的に排除しつつ、署名がどのドキュメント/Merkle状態に対するものかを暗号学的に証明できる。

### ⑤ 乙の原本完全性Assertion（Integrity Check）
*   乙のブラウザで暗号化PDFを復号した直後、**Web Worker内で** `crypto.subtle.digest('SHA-256', decryptedBytes)` を実行（メインスレッドのブロック回避）。
*   サーバーから取得した `original_sha256` と1ビットの狂いもなく一致するかバリデーション。
*   不一致時は「文書改ざんまたは通信破損警告」を表示して署名フローを即時凍結する。

### ⑥ 本人性確認（電子署名法・実務対応）とWebAuthnの法的解釈
*   **法的整理：** WebAuthn（FIDO2）は「端末ハードウェアでの生体/PIN操作」を保証する技術であり、単体では相手方の戸籍上の身元までは特定できません。そのため、本システムは**「メールOTP所持認証（本人特定） ＋ WebAuthn（物理的操作の担保） ＋ 事業者側タイムスタンプ・多重外部アンカー」による『立会人型（事業者署名型）の超強化モデル』**として運用・立証します。
*   **生体非対応端末へのフォールバック（認証レベル制度）：**
    *   **Level HIGH (`high_webauthn`)：** Touch ID / Face ID 等のプラットフォーム認証器（推奨）
    *   **Level MEDIUM (`medium_security_key`)：** YubiKey等のUSBセキュリティキー（cross-platform認証器）
    *   **Level LOW (`low_fallback`)：** メールOTP二段階認証のみ（WebAuthn非対応端末の最終手段）
    *   合意締結証明書PDFに認証レベルを明記し、法廷提出時に認証強度の判断材料を提供する。
    *   ~~画面上の手書きサイン~~ → 暗号学的バインドが存在せず法的証明力が低いため、フォールバック手段から**除外**する。
*   **耐量子の非対称性：** 甲側はML-DSA-65（耐量子）で署名しますが、乙側のWebAuthn認証器は現行仕様上ECDSA/RSA（非耐量子）で動作するハイブリッド構造であることを仕様に明記します。

### ⑦ デタッチド（分離型）契約パケットと納品仕様
*   **原本PDF：** 1バイトも改変せず不変。
*   **合意締結証明書PDF：** 締結完了時にサーバー（Node.js）で `pdf-lib` を用いて新規生成。
    *   原本の SHA-256 / SHA3-512 ハッシュ
    *   最終 Merkle Root ハッシュ
    *   甲の ML-DSA-65 耐量子公開鍵および署名
    *   乙の 本人性確認情報（メール検証）、WebAuthn 生体認証クレデンシャル情報および署名時刻
    *   **乙の認証レベル**（`high_webauthn` / `medium_security_key` / `low_fallback`）
    *   NICT HTTPS 原子時計タイムスタンプ
    *   OpenTimestamps カレンダー登録情報
    *   GitHub コミットハッシュリンク
*   **納品方法：**
    *   **乙の画面：** 非同期処理の完了をポーリングまたはWebSocketで検知後、インメモリに存在する原本PDFと生成された証明書PDFをJSZipでまとめ、**ZIP一括ダウンロード**を提供。大容量時はストリーミングZIP生成でメモリ消費を抑制。同時に鍵を乙のIndexedDBに安全キャッシュ。
    *   **通知メール：** 誰でも検証可能な**「合意締結証明書PDF」のみを添付**。原本の復号鍵はサーバーが保持しないため、メール本文には「原本はお手元の端末、または共有URLからアクセスしてください」と案内。

---

## 4. 実装上の落とし穴と完全対策（改訂版）

### 対策1：OpenTimestampsの「保留中（Pending）」と自動アップグレード
*   **課題：** 署名完了直後にはビットコインのトランザクションが確定しておらず、カレンダーサーバーの "Pending Receipt" しか取得できない。
*   **解決策：**
    1.  完了時点の証明書PDFには、Pending状態のルートハッシュと検証用ポータルURLを印字。
    2.  **定期バッチ（Cron）：** 署名完了から数時間後、バッチ処理で `ots upgrade` を実行し、確定したビットコインブロック情報を含む `.ots` 証明書をR2に自動上書き保存。
    3.  **証明書の再発行は不要：** 普遍のキーは「Final Merkle Root」であるため、静的検証ポータルにPDFを放り込んだ際に、ポータル側が最新の確定 `.ots` データを自動フェッチして照合完結できる設計とする。

### 対策2：Vercel環境での時刻取得（UDP NTP遮断への対応）と高可用性フォールバック
*   **課題：** Vercel（AWS Lambda）ではUDPポート123がブロックされているため、通常のNTPクライアントは即死する。また、単一の外部API（NICT）に依存すると可用性リスクがある。
*   **解決策：**
    *   **HTTPS（TCP 443）経由の多段フォールバック設計：**
        1.  **主系：NICT公式HTTP時刻API**（`https://ntp-a1.nict.go.jp/cgi-bin/json`）[タイムアウト: 1.5秒]
        2.  **副系1：Google Public NTP/HTTP API**（`https://time.google.com`）[タイムアウト: 1.5秒]
        3.  **副系2：Cloudflareエッジの `Date` ヘッダー**（自ドメインまたはCloudflare APIへのHEADリクエスト経由で取得）
    *   **リージョン最適化：** 締結・タイムスタンプ取得を行うVercel Functionsは**東京リージョン（`hnd1`）**に固定配置し、NICTサーバーとの通信レイテンシを極小化（10ms未満）してタイムアウトを防止する。
    *   取得時刻のソース（NICT / Google / Cloudflare）は監査ログの `payload_json` に明記される。

### 対策3：外部アンカーにおけるGitHubの位置づけ
*   **課題：** GitHubはリポジトリ管理者による履歴改変（force push）が可能なため、ビットコインと同等の絶対的改ざん耐性はない。
*   **解決策：**
    *   GitHubコミットは「人間がコミット履歴を即座にWebで視認できる補助的・迅速な状況証拠アンカー」と位置づけ、数学的・永続的な絶対証明はOpenTimestamps（ビットコイン）に担わせる。
    *   **GitHub実装詳細：** 専用の公開リポジトリ（例: `riffaegis-anchors`）に、ドキュメントIDと Final Merkle Root をコミットメッセージとして記録。ブランチ保護ルール（force push禁止）を設定するが、これは補助的措置に過ぎないことを明記する。

### 対策4：WebAuthnのアテステーション形式（attestation: 'none'）耐性
*   **課題：** SafariやChrome等のプライバシー保護により、CA証明書チェーンが欠落する場合がある。
*   **解決策：**
    *   CA証明書の厳密検証に依存せず、`clientDataJSON` の challenge（サーバーnonce ＋ 監査ハッシュの結合値）一致および公開鍵の署名整合性を合意の根拠とする。

### 対策5：個人情報保護法とメタデータ管理
*   `audit_logs` に記録されるIPアドレス、UserAgent、メールアドレスは個人情報に該当するため、利用規約・プライバシーポリシーに「合意成立の真正性証明のための監査証跡としての保存」を明記し、契約書本文とは別個に厳格な行単位セキュリティ（RLS）を適用する。

### 対策6：Vercel Serverless タイムアウトの回避（非同期処理分離）
*   **課題：** 締結処理（WebAuthn検証 → タイムスタンプ → OTS/GitHub → PDF生成 → R2保存）を1つの同期リクエストで処理すると、外部API応答次第でVercelの実行時間制限（Hobby: 10秒 / Pro: 60秒）を超過するリスクがある。
*   **解決策：**
    *   **同期フェーズ（即座にレスポンス）：** WebAuthn検証、DB保存（`CONSENT_RECEIVED`）、Merkle Root算出のみを同期処理。
    *   **非同期フェーズ（Background Functions / Inngest）：** NICT時刻取得、OTS/GitHubアンカリング、証明書PDF生成、R2保存を非同期キューで処理。各ステップはidempotent（冪等）に設計し、失敗時は指数バックオフでリトライ。
    *   乙の画面はWebSocketまたはShort Polling（5秒間隔 × 最大60回 = 5分）で完了を検知。タイムアウト時はメール通知にフォールバック。

### 対策7：Merkle Tree並行書き込みの整合性保証
*   **課題：** 同一ドキュメントに対して複数イベント（例: `IDENTITY_VERIFIED` と `VIEWED`）がほぼ同時に発生すると、`previous_log_hash` → `current_log_hash` チェーンが破壊される。
*   **解決策：**
    *   監査ログINSERT前に `pg_advisory_xact_lock(hashtext(document_id::text))` を取得し、同一ドキュメントへの書き込みを直列化する。
    *   トランザクション完了時に自動解放されるため、デッドロックリスクは最小限。

### 対策8：レート制限・不正利用防止
*   **OTP送信：** 同一メールアドレスに対し最大5回/時間。超過時は15分のクールダウン。
*   **署名URL生成：** 同一IPアドレスから最大20件/時間。
*   **WebAuthn認証試行：** 連続5回失敗でアカウント一時ロック（15分）。`signers.auth_attempts` で追跡。
*   **API全体：** Vercel Edge Middleware でグローバルレート制限（100 req/min/IP）を適用。

### 対策9：ドキュメントのキャンセル・失効フロー
*   **甲によるキャンセル：**
    *   `pending` ステータスのドキュメントのみキャンセル可能（`cancelled` へ遷移）。
    *   キャンセル理由を `documents.cancellation_reason` に記録し、`CANCELLED` イベントを監査ログに刻印。
    *   キャンセル後、既存の署名URLへアクセスした乙には「この署名依頼は取り消されました」画面を表示。
    *   R2上の暗号化データは即座には削除せず、リテンションポリシー（§6参照）に従い自動削除。
*   **自動失効：**
    *   `expires_at` を超過した `pending` ドキュメントは、定期バッチ（15分間隔）で `expired` に自動遷移。
    *   失効通知メールを甲に送信。

### 対策10：`navigator.credentials.create()` によるクレデンシャル管理方針
*   **課題：** ドキュメントごとに `create()` で新規クレデンシャルを生成すると、端末のクレデンシャルストレージが肥大化する。
*   **設計判断（セキュリティとUXのバランス）：**
    *   **乙（署名者）：** ドキュメントごとに `create()` で新規生成する。各署名が一意のクレデンシャルに紐づくことで、「どの端末でいつ合意したか」を個別に証明可能とする。署名頻度が低い（通常1回限り）ため、肥大化リスクは低い。
    *   **甲（作成者）：** 初回のみ `create()` でクレデンシャルを生成し、同一端末ではブラウザIndexedDBのクレデンシャルIDを用いて `get()` で既存認証器を再利用する。端末変更時はハードウェアバインド制約のため、新端末にて `create()` で再登録を行う（サーバー側で甲のML-DSA署名照合を経て更新）。

---

## 5. 法廷証拠力・検証エコシステム（裁判所・弁護士対応）

裁判官や相手方代理人がITの専門家でなくても、直感的に原本性と改ざんの有無を検証できるように**スタンドアローン検証ポータル**を無償公開します。

### ① 静的Web検証ポータル（`verify.riff-aegis.com`）
*   GitHub Pages等で完全静的（オープンソース）にホスト。
*   **オフライン基本検証（サーバー通信なし）：**
    1. 「原本PDF」と「合意締結証明書PDF」をブラウザ上にドラッグ＆ドロップ。
    2. ブラウザ内でSHA-256 / SHA3-512ハッシュを再計算。
    3. 証明書内に記載されたハッシュ値、甲のML-DSA署名、Merkle Treeチェーンを自動検証。
*   **オンライン拡張検証（外部API通信あり）：**
    4. OpenTimestampsの最新確定ブロック（.ots）をBitcoinノード / OTSサーバーから取得し照合。
    5. GitHub公開コミットの存在をAPI経由で確認。
*   **出力：** 「改ざんなし・合意成立証明レポート（裁判提出用A4サマリー）」をワンクリック印刷・PDF保存可能。オフライン検証のみ実施した場合はその旨をレポートに明記し、OTS/GitHub照合の結果欄は「未確認（オンライン検証で確認可能）」と表示する。

---

## 6. データリテンションポリシー（保存期間・自動削除）

| データ区分 | 保存期間 | 根拠 |
|-----------|---------|------|
| 初期化未確認（`initialized`）ドキュメント | 作成後 **30分** | アップロード中断・通信切断による孤立レコードのパージ |
| 暗号化原本PDF（R2） | 締結完了後 **10年** | 商事債権の消滅時効（民法166条: 10年）に対応 |
| 合意締結証明書PDF（R2） | **無期限** | 公開検証可能な証拠として永続保存 |
| OTS確定証明書 `.ots`（R2） | **無期限** | ビットコインアンカーの検証に必要 |
| `documents` レコード | 締結完了後 **10年** | 暗号化原本と同期して削除 |
| `audit_logs` レコード | 締結完了後 **10年** | 監査証跡として原本と同期保存 |
| `signers` 個人情報 | 締結完了後 **10年**（その後匿名化） | 個人情報保護法の利用目的達成後は匿名化 |
| `expired` / `cancelled` ドキュメント | ステータス遷移後 **90日** | 未締結文書の長期保存は不要 |
| `idempotency_keys` レコード | 作成後 **30分** | API多重リクエスト検知期間終了後のパージ |

*   **自動削除バッチ：** 毎日深夜にVercel Cron / GitHub Actionsで実行。保存期限超過データを自動パージ。孤立 `initialized` は15分ごとのCronでパージ。
*   **匿名化処理：** 10年経過した `signers` レコードのメールアドレス・IPアドレスをハッシュ値に置換し、個人を特定不可能にする。監査ログの整合性（Merkle Tree）は元データのハッシュ値がチェーンに刻まれているため、匿名化後も検証可能。

---

## 7. APIエンドポイント設計

### 認可モデル・CORS・冪等性

本システムは永続的なユーザーアカウントを持たないため、Supabase Authは使用しません。認可は以下のJWTトークン階層で厳格に分離・制御します：

*   **甲（作成者 - ドキュメント単位）：`creator_document_token`**（JWT、24時間有効）
    *   クレーム：`sub: "creator:doc:{document_id}"`, `document_id`, `ml_dsa_public_key_hash`, `role: "creator"`
    *   用途：特定ドキュメントのアップロード確認、WebAuthn登録、アクティベーション、キャンセル。
*   **甲（作成者 - ダッシュボード・ID単位）：`creator_identity_token`**（JWT、24時間有効）
    *   クレーム：`sub: "creator:id:{ml_dsa_public_key_hash}"`, `ml_dsa_public_key_hash`, `role: "creator_admin"`
    *   用途：`/api/documents`（ドキュメント一覧取得）、全管理ドキュメントの横断閲覧。
*   **乙（署名者）：`signer_session_token`**（JWT、**3時間有効**、サイレント更新可能）
    *   クレーム：`sub: "signer:{token}"`, `document_id`, `signer_id`, `email_verified`, `role: "signer"`
    *   用途：暗号化原本ダウンロードURL取得、WebAuthn合意・拒否処理。
*   **Supabase接続：** Vercel APIは `service_role` キーを使用。クライアントは `anon` キーでステータス専用テーブル（`documents_realtime_status`）のみ購読。
*   **CORSポリシー（全API共通）：**
    ```http
    Access-Control-Allow-Origin: https://app.riff-aegis.com
    Access-Control-Allow-Methods: GET, POST, OPTIONS
    Access-Control-Allow-Headers: Content-Type, Authorization, Idempotency-Key
    Access-Control-Max-Age: 86400
    ```
*   **冪等性（Idempotency）：** 作成・合意等の更新系エンドポイントは `Idempotency-Key: {UUIDv4}` ヘッダーをサポートし、30分以内の同一キー再送時は前回の処理結果を即時返却。

### ① ドキュメント作成フロー（甲）

```
POST /api/documents/init
  Headers: Idempotency-Key: {UUIDv4}
  Body: { original_sha256, original_sha3_512, file_size_bytes, creator_email,
          creator_ml_dsa_public_key, creator_ml_dsa_signature }
  Response: { document_id, r2_presigned_upload_url,
              creator_document_token, creator_identity_token }
  備考: file_size_bytes <= 20MBを検証。ステータス INITIALIZED で作成。
        R2プリサインドURLを返却し、甲がブラウザから直接R2へアップロードする
        （Vercel Serverlessのボディサイズ制限4.5MBを回避）

POST /api/documents/:id/confirm-upload
  Headers: Authorization: Bearer {creator_document_token}
  Body: { upload_completed: true }
  Response: { status: "uploaded" }
  備考: R2側でオブジェクトの存在とサイズを検証し、upload_confirmed=trueに更新

POST /api/documents/:id/webauthn/register-challenge
  Headers: Authorization: Bearer {creator_document_token}
  Response: { challenge, rp_id, rp_name, user_id, timeout }
  備考: challenge = HMAC-SHA-256(server_nonce, original_sha256 || ml_dsa_public_key)
        server_nonceをDBに一時保存

POST /api/documents/:id/webauthn/register-complete
  Headers: Authorization: Bearer {creator_document_token}
  Body: { credential_id, attestation_object, client_data_json }
  Response: { verified: true }
  備考: challenge内nonceを照合後、nonceを消去。creator_webauthn_bindingを保存

POST /api/documents/:id/activate
  Headers: Authorization: Bearer {creator_document_token}
  Body: { keystore_backup_confirmed: true }
  Response: { signing_token, signing_url_template, status: "pending" }
  備考: .riffkeyバックアップ完了を確認後にステータスを PENDING に遷移し署名トークンを発行。
        signing_url_template = "https://app.riff-aegis.com/sign/{token}#{key}"
```

### ② 署名フロー（乙）

```
GET /api/sign/:token/info
  Response: { document_id, original_sha256, original_sha3_512,
              creator_ml_dsa_public_key, creator_ml_dsa_signature,
              status, requires_otp: true }
  備考: 認証前情報取得。暗号化PDFのダウンロードURLはセキュリティ保護のため返却しない。

POST /api/sign/:token/otp/send
  Body: { email }
  Response: { otp_sent: true, expires_in_seconds: 600 }
  備考: レート制限: 同一email 5回/時間

POST /api/sign/:token/otp/verify
  Body: { email, otp_code }
  Response: { verified: true, signer_session_token, r2_presigned_download_url }
  備考: 5回連続失敗でロック（15分）。成功時にJWT（3時間有効）と
        暗号化PDFの一時ダウンロードURL（15分有効）を発行。

GET /api/sign/:token/download-url
  Headers: Authorization: Bearer {signer_session_token}
  Response: { r2_presigned_download_url }
  備考: 閲覧中の再ダウンロードやURL期限切れ時の再取得用（15分有効）

POST /api/sign/:token/webauthn/challenge
  Headers: Authorization: Bearer {signer_session_token}
  Response: { challenge, rp_id, rp_name, timeout }
  備考: challenge = HMAC-SHA-256(server_nonce, merkle_latest_node_hash)

POST /api/sign/:token/consent
  Headers: Authorization: Bearer {signer_session_token}
           Idempotency-Key: {UUIDv4}
  Body: { credential_id, attestation_object, client_data_json, auth_level }
  Response: { accepted: true, status: "consent_received", poll_url }
  備考: 同期処理（WebAuthn検証・DB保存・Merkle Root算出）完了後、
        非同期キューへ後続処理をディスパッチ

POST /api/sign/:token/reject
  Headers: Authorization: Bearer {signer_session_token}
  Body: { reason }
  Response: { rejected: true }
  備考: REJECTEDイベントを監査ログに刻印、甲へ通知メール送信
```

### ③ ステータス・ダウンロード

```
GET /api/documents/:id/status
  Headers: Authorization: Bearer {creator_document_token | creator_identity_token | signer_session_token}
  Response: { status, certificate_ready, ots_status, updated_at }
  備考: ポーリング用。リアルタイム検知は documents_realtime_status テーブルの購読を推奨

GET /api/documents/:id/certificate
  Headers: Authorization: Bearer {creator_document_token | creator_identity_token | signer_session_token}
  Response: { certificate_pdf_presigned_url, ots_proof_presigned_url }
  備考: 合意締結証明書のダウンロードURL（15分有効）
```

### ④ 管理・運用（甲）

```
GET /api/documents
  Headers: Authorization: Bearer {creator_identity_token}
  Query: ?status=pending&page=1&limit=20
  Response: { documents: [...], total, page }
  備考: 甲が作成した全ドキュメント一覧。JWT内の ml_dsa_public_key_hash で安全に絞り込み

POST /api/documents/:id/cancel
  Headers: Authorization: Bearer {creator_document_token | creator_identity_token}
           Idempotency-Key: {UUIDv4}
  Body: { reason }
  Response: { cancelled: true }
  備考: pendingステータスのみキャンセル可能

POST /api/identity/restore
  Body: { ml_dsa_public_key, restore_challenge_signature }
  Response: { creator_identity_token }
  備考: .riffkey復元フロー。クライアント側で復号したML-DSA秘密鍵でサーバーchallengeに署名し、
        甲のIDを確認して creator_identity_token を発行。

POST /api/identity/rebind-webauthn
  Headers: Authorization: Bearer {creator_identity_token}
  Body: { new_credential_id, new_attestation_object, new_client_data_json }
  Response: { rebind_completed: true }
  備考: 端末移行時、新端末のWebAuthnクレデンシャルをML-DSAアイデンティティに再バインド
```

### ⑤ 内部・バッチ処理

```
POST /api/internal/finalize  (Inngest function - Region: hnd1)
  Trigger: documents.consent_received イベント
  処理: NICT時刻取得（3段フォールバック） → OTS/GitHubアンカリング → 証明書PDF生成 → R2保存
  各ステップはidempotent。失敗時は指数バックオフ (10s → 30s → 90s)

POST /api/cron/expire-documents  (Vercel Cron: */15 * * * *)
  Headers: Authorization: Bearer {CRON_SECRET}
  処理: 1. expires_at超過のpendingドキュメントをexpiredに遷移、甲へ通知
        2. 30分間confirm-uploadされなかった孤立initializedドキュメントを自動パージ

POST /api/cron/upgrade-ots  (Vercel Cron: 0 */3 * * *)
  Headers: Authorization: Bearer {CRON_SECRET}
  処理: ots_status=pendingのドキュメントに対しots upgradeを実行

POST /api/cron/cleanup  (Vercel Cron: 0 3 * * *)
  Headers: Authorization: Bearer {CRON_SECRET}
  処理: リテンションポリシーに基づく自動削除・匿名化（idempotency_keys, 90日超過失効文書等）
```

### ⑥ 共通エラーレスポンス形式

```json
{
  "error": {
    "code": "DOCUMENT_EXPIRED",
    "message": "この署名依頼は期限切れです。",
    "details": {}
  }
}
```

| HTTPステータス | エラーコード | 説明 |
|---------------|------------|------|
| 400 | `INVALID_REQUEST` | バリデーションエラー |
| 401 | `UNAUTHORIZED` | トークン無効・期限切れ |
| 403 | `FORBIDDEN` | 権限不足（他人のドキュメント等） |
| 404 | `DOCUMENT_NOT_FOUND` | ドキュメント/トークンが存在しない |
| 409 | `ALREADY_SIGNED` | 既に署名済み |
| 409 | `ALREADY_CANCELLED` | 既にキャンセル済み |
| 410 | `DOCUMENT_EXPIRED` | 署名依頼が期限切れ |
| 423 | `ACCOUNT_LOCKED` | 認証試行回数超過でロック中 |
| 429 | `RATE_LIMITED` | レート制限超過 |
| 500 | `INTERNAL_ERROR` | サーバー内部エラー |
| 503 | `EXTERNAL_SERVICE_UNAVAILABLE` | 外部API（NICT/OTS/GitHub）障害 |

---

## 8. フロントエンドページ構成・ルーティング

### ① 画面一覧（Next.js App Router）

```
app/
├── page.tsx                          # ランディングページ
├── layout.tsx                        # 共通レイアウト（ヘッダー・フッター）
│
├── new/                              # ドキュメント作成（甲）
│   ├── page.tsx                      #   Step 1: PDF選択・暗号化・アップロード
│   ├── authenticate/page.tsx         #   Step 2: WebAuthn登録 & .riffkey保存
│   └── share/page.tsx                #   Step 3: 署名URL発行・共有画面
│
├── documents/                        # ダッシュボード（甲）
│   ├── page.tsx                      #   ドキュメント一覧（ステータス別フィルター）
│   └── [id]/page.tsx                 #   ドキュメント詳細（監査ログ・証明書DL）
│
├── sign/                             # 署名フロー（乙）
│   └── [token]/page.tsx              #   マルチステップ署名画面（SPA内遷移）
│
├── restore/page.tsx                  # .riffkeyからのID復元（甲）
│
└── api/                              # APIルート（§7参照）
    ├── documents/
    ├── sign/
    ├── identity/
    ├── internal/
    └── cron/
```

### ② 甲（作成者）のユーザーフロー

```text
[ランディング] ──► [/new] PDF選択（%PDF-マジックバイト検証）・プレビュー確認
                         │
                         ▼ クライアントサイド処理:
                         │  1. crypto.subtle.generateKey(AES-256-GCM)
                         │  2. PDF暗号化 (Web Worker)
                         │  3. SHA-256 + SHA3-512 ハッシュ計算
                         │  4. ML-DSA-65 鍵生成 or IndexedDBから復元
                         │  5. ML-DSA-65 署名生成
                         │  6. POST /api/documents/init (Idempotency-Key付き)
                         │     → creator_document_token & creator_identity_token を受領
                         │  7. R2プリサインドURLへ直接アップロード
                         │  8. POST /api/documents/:id/confirm-upload
                         ▼
                   [/new/authenticate] WebAuthn登録
                         │  1. POST /api/.../register-challenge (HMAC方式)
                         │  2. navigator.credentials.create()
                         │  3. POST /api/.../register-complete
                         │  4. .riffkey生成（Argon2id: デスクトップ64MB / モバイル32MB）
                         │  5. ダウンロード必須 → 完了まで次へ進めない
                         ▼
                   [/new/share] 署名URL発行
                         │  POST /api/documents/:id/activate
                         │  「URL＋鍵の一括コピー」ボタン
                         │  「鍵単体コピー」ボタン
                         │  🔒 暗号化メッセンジャー推奨ガイダンス
                         │  ⚠️ 鍵消失警告
                         ▼
                   [/documents] ダッシュボード（creator_identity_tokenで一覧追跡）
```

### ③ 乙（署名者）のユーザーフロー

```text
[/sign/:token] マルチステップ署名画面（SPA内遷移・ページ離脱なし）

   Step 1: 本人確認（事前ダウンロード防止）
   │  GET /api/sign/:token/info（文書メタ情報のみ取得。PDFのURLは未返却）
   │  メールアドレス入力 → POST /api/sign/:token/otp/send
   │  OTPコード入力 → POST /api/sign/:token/otp/verify
   │  ※ OTP認証成功時に signer_session_token (3時間有効) と
   │     暗号化PDFのプリサインドダウンロードURLを取得
   ▼
   Step 2: 文書確認
   │  URLフラグメント(#)から復号鍵を取得
   │  ※ 鍵が見つからない場合: 手動貼り付けフォームを表示
   │  R2から暗号化PDFをダウンロード（OTP認証後の一時URL）
   │  Web Workerで復号 → SHA-256再計算 → 原本ハッシュ照合
   │  ※ 不一致: 「改ざん警告」表示 → フロー凍結
   │  一致確認後、PDFビューワーで全文表示
   ▼
   Step 3: 合意 or 拒否
   │  ├── [合意] WebAuthn実行
   │  │   POST /api/sign/:token/webauthn/challenge (HMAC方式)
   │  │   navigator.credentials.create()
   │  │   POST /api/sign/:token/consent (Idempotency-Key付き)
   │  │
   │  └── [拒否] 理由入力ダイアログ
   │      POST /api/sign/:token/reject
   ▼
   Step 4: 完了（合意時のみ）
      「証明書を準備中です...」プログレス表示
      documents_realtime_status 購読 or ポーリングで完了検知
      完了後: 原本PDF + 証明書PDF → JSZip → 自動ダウンロード
      復号鍵をIndexedDBにキャッシュ
```

### ④ 主要クライアントサイドコンポーネント

| コンポーネント | 用途 | 主な依存 |
|--------------|------|---------|
| `PdfDropzone` | PDF選択UI（D&D対応） | react-dropzone |
| `PdfViewer` | インメモリPDF描画 | pdf.js (pdfjs-dist) |
| `CryptoWorker` | 暗号化・ハッシュ計算・ML-DSA処理 | Web Worker + @noble/* |
| `WebAuthnFlow` | WebAuthn登録/認証UI | @simplewebauthn/browser |
| `KeystoreManager` | .riffkey生成・復元UI | hash-wasm (Argon2id) |
| `OtpInput` | OTPコード入力フォーム | カスタム実装 |
| `SigningUrlCard` | URL表示・コピー・鍵管理 | カスタム実装 |
| `AuditTimeline` | 監査ログのタイムライン表示 | カスタム実装 |
| `StatusPoller` | 非同期処理の完了検知 | @supabase/supabase-js (Realtime) |
| `ZipDownloader` | ZIP生成・ダウンロード | JSZip |

---

## 9. 認可戦略・Supabase RLS

### ① アクセスパターン

```text
[甲ブラウザ] ──REST──► [Vercel API (service_role)] ──SQL──► [Supabase PostgreSQL]
[乙ブラウザ] ──REST──► [Vercel API (service_role)] ──SQL──► [Supabase PostgreSQL]
[甲/乙ブラウザ] ──WS──► [Supabase Realtime (anon key)] ──RLS──► [documents_realtime_status のみ]
```

クライアントがSupabaseに直接アクセスするのは**Realtimeのステータス購読のみ**です。原本ハッシュ・鍵・署名・個人情報を含むベーステーブルへの直接アクセスは全遮断し、ステータス同期専用テーブル `documents_realtime_status` のみ購読を許可します。

### ② RLSポリシー定義

```sql
-- 全テーブルでRLSを有効化
ALTER TABLE documents ENABLE ROW LEVEL SECURITY;
ALTER TABLE signers ENABLE ROW LEVEL SECURITY;
ALTER TABLE audit_logs ENABLE ROW LEVEL SECURITY;
ALTER TABLE idempotency_keys ENABLE ROW LEVEL SECURITY;
ALTER TABLE documents_realtime_status ENABLE ROW LEVEL SECURITY;

-- 【漏洩防止】documents / signers / audit_logs / idempotency_keys:
-- anon ロールからのアクセスは一切禁止（service_role のみがアクセス可能）
CREATE POLICY "documents_deny_anon" ON documents
  FOR ALL TO anon
  USING (false);

CREATE POLICY "signers_deny_anon" ON signers
  FOR ALL TO anon
  USING (false);

CREATE POLICY "audit_logs_deny_anon" ON audit_logs
  FOR ALL TO anon
  USING (false);

CREATE POLICY "idempotency_deny_anon" ON idempotency_keys
  FOR ALL TO anon
  USING (false);

-- 【Realtime専用テーブル】最小限のステータス（id, document_id, status, certificate_ready, updated_at）のみ
-- anon ロールからのSELECTを許可（クライアントは document_id でフィルタして購読）
CREATE POLICY "realtime_status_read_anon" ON documents_realtime_status
  FOR SELECT TO anon
  USING (true);

-- service_role はRLSを自動バイパスするため、Vercelバックエンドは制限なく処理可能
```

### ③ JWT（セッショントークン）仕様

認可スコープの矛盾を解消するため、トークンを用途別に分離します。

```json
// ① creator_document_token (甲・特定ドキュメント操作用)
{
  "sub": "creator:doc:{document_id}",
  "document_id": "9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d",
  "ml_dsa_public_key_hash": "e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855",
  "role": "creator",
  "iat": 1773532800,
  "exp": 1773619200  // 24時間有効
}

// ② creator_identity_token (甲・ダッシュボード／横断管理用)
{
  "sub": "creator:id:{ml_dsa_public_key_hash}",
  "ml_dsa_public_key_hash": "e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855",
  "role": "creator_admin",
  "iat": 1773532800,
  "exp": 1773619200  // 24時間有効（.riffkey再認証で再発行可能）
}

// ③ signer_session_token (乙・署名操作用)
{
  "sub": "signer:{token}",
  "document_id": "9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d",
  "signer_id": "a2c3d4e5-f6a7-8b9c-0d1e-2f3a4b5c6d7e",
  "email_verified": "signer@example.com",
  "role": "signer",
  "iat": 1773532800,
  "exp": 1773543600  // 3時間有効（精読・離席に十分な猶予時間を確保）
}
```

*   署名アルゴリズム：HS256（`JWT_SECRET` 環境変数で署名）
*   Vercel APIミドルウェアで全保護エンドポイントのJWT署名・有効期限・スコープ検証を実行。

---

## 10. メール送信基盤

### ① サービス選定

**Resend** を採用（Vercelとの親和性、シンプルなAPI、Reactメールテンプレート対応）。

### ② メールテンプレート一覧

| テンプレート | トリガー | 宛先 | 内容 |
|------------|---------|------|------|
| `otp-verification` | OTP送信リクエスト | 乙 | 6桁OTPコード、10分有効、リクエスト元IP表示 |
| `signing-completed` | ステータスCOMPLETED | 甲・乙 | 合意締結証明書PDF添付、検証ポータルリンク |
| `signing-rejected` | 乙が拒否 | 甲 | 拒否理由、ドキュメント名 |
| `document-expired` | 自動失効時 | 甲 | 失効通知、再作成の案内 |
| `document-cancelled` | 甲がキャンセル | 甲 | キャンセル確認 |
| `admin-alert` | ADMIN_REVIEW遷移時 | 運営者 | 障害詳細、ドキュメントID、最終到達ステート |
| `ots-upgraded` | OTS確定時（任意） | 甲・乙 | ビットコインブロック確定通知（オプトイン） |

### ③ メール送信制約

*   送信元: `noreply@riff-aegis.com`（カスタムドメインDKIM/SPF設定済み）
*   合意締結証明書PDFの添付サイズ上限: **10MB**（超過時はダウンロードリンクのみ）
*   OTPメールには「このリクエストに心当たりがない場合は無視してください」を必ず記載。

---

## 11. 環境変数・シークレット一覧

### ① Vercel環境変数

| 変数名 | 用途 | 公開範囲 |
|--------|------|---------|
| `NEXT_PUBLIC_SUPABASE_URL` | Supabase API URL | クライアント公開 |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Supabase anonキー（Realtime用） | クライアント公開 |
| `NEXT_PUBLIC_APP_URL` | アプリ本体URL（URL生成用） | クライアント公開 |
| `SUPABASE_SERVICE_ROLE_KEY` | Supabase管理キー | サーバーのみ |
| `JWT_SECRET` | セッションJWT署名鍵（32バイト以上） | サーバーのみ |
| `CLOUDFLARE_R2_ACCOUNT_ID` | R2アカウントID | サーバーのみ |
| `CLOUDFLARE_R2_ACCESS_KEY_ID` | R2 APIトークンID | サーバーのみ |
| `CLOUDFLARE_R2_SECRET_ACCESS_KEY` | R2 APIトークンシークレット | サーバーのみ |
| `CLOUDFLARE_R2_BUCKET_NAME` | R2バケット名 | サーバーのみ |
| `CLOUDFLARE_R2_ENDPOINT` | R2エンドポイントURL | サーバーのみ |
| `RESEND_API_KEY` | Resend APIキー | サーバーのみ |
| `EMAIL_FROM` | 送信元メールアドレス | サーバーのみ |
| `GITHUB_TOKEN` | GitHubアンカーコミット用PAT | サーバーのみ |
| `GITHUB_ANCHOR_REPO` | アンカーリポジトリ（例: `org/riffaegis-anchors`） | サーバーのみ |
| `NICT_API_URL` | NICT時刻API URL | サーバーのみ |
| `INNGEST_EVENT_KEY` | Inngestイベントキー | サーバーのみ |
| `INNGEST_SIGNING_KEY` | Inngest署名キー | サーバーのみ |
| `CRON_SECRET` | Cronエンドポイント認証キー | サーバーのみ |
| `ADMIN_EMAIL` | 運営者通知先メール | サーバーのみ |
| `ADMIN_SLACK_WEBHOOK_URL` | 運営者Slack通知（任意） | サーバーのみ |

### ② シークレットのローテーション方針

*   `JWT_SECRET`：6ヶ月ごとにローテーション。旧鍵での検証を24時間維持（グレースピリオド）。
*   `GITHUB_TOKEN`：Fine-grained PAT、`riffaegis-anchors` リポジトリの `contents: write` 権限のみ。90日有効で自動ローテーション。
*   `CLOUDFLARE_R2_*`：APIトークンスコープは対象バケットの `object read/write` のみに限定。

---

## 12. 依存パッケージ一覧

### ① サーバーサイド（Node.js / Vercel Functions）

| パッケージ | 用途 | 備考 |
|-----------|------|------|
| `next` | フレームワーク | App Router使用 |
| `@supabase/supabase-js` | PostgreSQL接続 | service_role接続 |
| `@aws-sdk/client-s3` | Cloudflare R2操作 | S3互換API、プリサインドURL生成 |
| `@aws-sdk/s3-request-presigner` | プリサインドURL生成 | upload/download用 |
| `@simplewebauthn/server` | WebAuthn検証 | アテステーション/アサーション検証 |
| `pdf-lib` | 合意締結証明書PDF生成 | 日本語フォント埋め込み必要 |
| `opentimestamps` | OTSスタンプ・アップグレード | カレンダーサーバー通信 |
| `octokit` | GitHub API | アンカーコミット作成 |
| `resend` | メール送信 | トランザクショナルメール |
| `@react-email/components` | メールテンプレート | Reactベースのメール構築 |
| `inngest` | 非同期ジョブキュー | 締結エンジン非同期処理 |
| `jose` | JWT生成・検証 | セッションJWT |
| `@noble/hashes` | SHA3-512ハッシュ | サーバー側Merkle Tree計算 |

### ② クライアントサイド（ブラウザ）

| パッケージ | 用途 | 備考 |
|-----------|------|------|
| `@noble/post-quantum` | ML-DSA-65 署名生成/検証 | 純JS実装、Web Worker推奨 |
| `@noble/hashes` | SHA3-512ハッシュ | ブラウザ側ハッシュ計算 |
| `@simplewebauthn/browser` | WebAuthn登録/認証 | ブラウザ側WebAuthn操作 |
| `hash-wasm` | Argon2id KDF | .riffkeyパスフレーズ導出（WASM高速） |
| `pdfjs-dist` | PDFインメモリ描画 | Mozilla pdf.js |
| `jszip` | ZIP生成 | 原本＋証明書の一括DL |
| `idb-keyval` | IndexedDB簡易操作 | ML-DSA鍵・復号鍵キャッシュ |
| `react-dropzone` | ファイルD&D UI | PDF選択UI |

### ③ 開発・品質管理

| パッケージ | 用途 |
|-----------|------|
| `typescript` | 型安全 |
| `vitest` | ユニットテスト |
| `playwright` | E2Eテスト（WebAuthnモック含む） |
| `eslint` + `prettier` | コード品質 |
| `@openapitools/openapi-generator-cli` | APIスキーマからの型生成（任意） |

> [!NOTE]
> **pdf-libの日本語対応：** pdf-libはデフォルトでは日本語フォントを埋め込めないため、NotoSansJP等のフォントファイル（.ttf）をプロジェクトに同梱し、`embedFont()` で埋め込む必要がある。証明書PDFの日本語レンダリングに必須。